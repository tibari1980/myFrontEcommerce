import { Category } from './category';
export class Product {
    code:number;
    name:string;
    description:string;
   currentPrice:number;
    photoName:string;
   available: boolean;
   selected: boolean
    promotion:boolean;
}
